// Shared frontend component foundation for apps/web.
// Deliberately empty — no components exist yet; this package is scaffolding only until
// Next.js/React are actually configured in apps/web (out of scope for this step).
export {};
