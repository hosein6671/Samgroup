# AI Context — Behavioral Rules & Project State

This file is the companion to [`CLAUDE.md`](./CLAUDE.md). Where `CLAUDE.md` is the map (what exists, where to find it), this file is the rulebook for _how to act_ on this specific project, plus the state that only exists because of this project's decision history — not derivable by reading the docs cold. General coding hygiene (naming, TypeScript strictness, comment discipline) lives in [`docs/AI_RULES.md`](./docs/AI_RULES.md) and is not repeated here.

---

## Workflow conventions established on this project

- **Documentation changes and code changes are separate approval gates.** Approval to update docs, or to produce a plan, is not approval to write application code, run installs, or scaffold frameworks — even immediately after a plan was approved. Always get an explicit, separate go-ahead before the first line of actual code.
- **Architecture-affecting proposals get an ADR**, not just a prose edit — see `docs/ADR/`. If you're about to change something that already has an ADR, that's a signal to stop and confirm with the user rather than silently overriding it.
- **Verify third-party tool claims before they inform a decision.** ADR-002 (two databases instead of one shared schema) exists because an initial design was checked against Payload CMS's actual documentation/issue tracker and turned out to rely on an experimental, bug-prone feature. Don't take a library's capability on faith when a real architectural decision depends on it — check the source.
- **When a request conflicts with something already frozen, say so and ask — don't quietly comply or quietly refuse.** Surface the tension (as was done for the Vercel/VPS deployment split below) rather than resolving it unilaterally.

## Absolute constraints

- Never replace or suggest replacing **Payload CMS**, **NestJS**, **Prisma/PostgreSQL**, or **pnpm + Turborepo**. These are frozen (see `CLAUDE.md` and ADR-001/002/003).
- Never introduce or reference **Sanity CMS**, or any CMS other than Payload.
- Never use Payload's `schemaName` adapter option to share a database with Prisma — two independent databases (`sam_platform`, `sam_cms`) is the frozen decision (ADR-002).
- Never let `apps/web` call Payload's API directly — NestJS is the only API surface the frontend talks to (ADR-003). **This includes the Admin Dashboard**, which is an area inside `apps/web`, not an exception: it calls `/api/v1/admin/*` only, never Payload, never a database.
- Never create a fourth application. The Admin Dashboard lives inside `apps/web` as a separate route area (`app/(admin)/admin/*`, outside the `[locale]` segment) — approved decision; `apps/admin` was explicitly rejected.
- Don't add speculative infrastructure or abstractions for future phases (Customer Portal, CRM, Workflow, ERP Integration, AI/RAG, etc.) beyond what `docs/DATA_MODEL.md §2` already anchors. Future-phase design happens when that phase starts, not now.
- If AI/RAG capabilities are ever built, they must never query `sam_platform`/`sam_cms`/MinIO directly — only through the existing NestJS API, and RAG's own vector store must be its own isolated database, never grafted onto an existing one. Full rationale: `docs/ai/RAG_ARCHITECTURE.md §0`.

## Known open threads (not yet resolved — don't assume either direction)

- **RESOLVED — deployment target is one VPS, Docker only.** Confirmed by the user on 7 August 2026: the entire platform (`apps/web`, `apps/api`, `apps/cms`, PostgreSQL, MinIO) deploys as Docker containers on a **single Linux VPS**, orchestrated by Docker Compose behind Nginx. The earlier Vercel split-hosting proposal is **dropped** — don't reintroduce it or any external hosting provider. Reconciled across `TECH_STACK.md`, `ARCHITECTURE.md`, `DEVOPS.md`, `technology/FRONTEND_STACK.md`, and `seo/SEO_ARCHITECTURE.md`; all five are now authoritative and consistent on deployment topology. Two consequences worth carrying forward: `web` and `api` share one origin behind Nginx (no CORS configuration between them), and there is no third-party edge/CDN tier, so the Core Web Vitals budget must be met from the origin. Recorded as **ADR-005** (`docs/ADR/ADR-005-vps-docker-deployment.md`) — treat it as frozen alongside ADR-001/002/003/004. **Not yet built:** the Docker and GitHub Actions files themselves.
- **i18n is fully architected and decided, except one item.** [docs/i18n/INTERNATIONALIZATION_STRATEGY.md](./docs/i18n/INTERNATIONALIZATION_STRATEGY.md) covers URL strategy (locale list is data, in a `Locale` table, not code), Payload's native localization for Payload-owned content, a `ContentTranslation` table for Prisma-owned content (Product/Category/BlogPost), hreflang/localized sitemaps, RTL/LTR, and the steps to add a language. **Decided**: default locale `en`; launch locales `en`+`fa`+`ar` together (not phased); future locales added by configuration + content only; translation workflow is hybrid (machine-assisted drafts allowed everywhere, human review required before publish for product specs, company/legal/contact content, and lead-generating form copy — tracked via `ContentTranslation.translationStatus`). **Still open**: RTL typeface pairing only — the chosen Latin typefaces in `docs/design/FRONTEND_DESIGN_DIRECTION.md` have no Arabic/Persian glyph coverage; candidates proposed, needs design-team sign-off.
- **Mapbox's placement is now confirmed; Three.js's is still not.** The richer "Completed" site structure (see below) explicitly calls for an interactive/illustrated map on Export & Logistics' Global Reach section — that resolves half of what was previously one open thread. Three.js/R3F/Drei still has no confirmed placement anywhere in the actual content — packaging visualization remains a proposal, not a requirement.
- **RESOLVED — `SampleRequest` no longer exists.** Merged into `Inquiry` (`inquiryType: 'Sample Request'` + `relatedProductId`). If you see `SampleRequest` referenced anywhere, that reference is stale. Don't reintroduce the entity.
- **RESOLVED — closed by the user directly.** `Sam Group Website Structure - COMPLETED.xlsx` is confirmed by the user as the source document already reviewed and reconciled into `docs/SITE_STRUCTURE.md` (copied into the repo as `docs/content/Sam Group Website Structure - Completed.xlsx`). It's a 23-sheet, launch-ready expansion of the original 7-sheet file (27-page sitemap, full per-category product pages, Quality & Certifications, Blog & Insights editorial plan, FAQ, Careers & Partners, Legal Pages, Global Components, an SEO Master sheet). This is now the frozen source-of-truth pairing: **`Completed.xlsx` (source) + `docs/SITE_STRUCTURE.md` (engineering reference)**, both superseding `_v1`/`_v2`, which are historical-only. No further file comparison needed — don't reopen this.
- **RESOLVED — Prisma data-model gaps are catalogued, decided, and reflected in `DATA_MODEL.md`/`DATABASE.md`** (now authoritative; `docs/DATA_MODEL_GAP_REVIEW.md` is retained as the analysis record). Approved outcomes: `SampleRequest` merged into `Inquiry`; `JobApplication` is **Admin-only** with no `assignedToId` (CVs never reach Sales queues, and it must never be indexed by RAG); `NewsletterSubscription` is a Phase 1 Prisma entity with double opt-in; download gating covers **catalogues only**, never TDS/SDS; `CustomFormulationRequest`'s missing `email`/`phone` fields (a functional defect — submissions were unanswerable) are fixed. Personal-data retention is recorded as a requirement in `SECURITY.md`; **concrete retention periods still need legal input** and are the one genuinely open item here.
- **SUPERSEDED — see [ADR-007](./docs/ADR/ADR-007-product-taxonomy-v2.md).** Kept as the record of what was decided before it. Original entry: _"**Product/Category routing confirmed, differs from an earlier assumption**: each of the six product categories is one rich page (grades/SKUs listed as sections within it), not a category-listing-plus-per-SKU-detail-page structure. `docs/frontend/FRONTEND_ARCHITECTURE.md` §1 was updated accordingly — no `[productSlug]` route."_ The six category pages half of that still holds; the no-detail-route half does not.
- **RESOLVED — Product Detail routes are architecturally approved (ADR-007, accepted 12 August 2026).** The canonical Product Detail URL is **`/{locale}/products/{product-slug}`** — flat, with Segment and Product Type as navigation/facet relationships rather than URL ancestry, so a Product belonging to several Segments still has exactly one canonical URL. Product Taxonomy v2 alongside it: Product Family stays single-valued per Product, Segment is first-class and many-to-many with Product, Product Type is first-class and shared across Segments, and a Product carries one primary Product Type initially (many-to-many deferred). The six existing Product Family pages remain valid — Segment navigation is an additional axis, not a replacement. **Implementation status — read this precisely, because "the taxonomy" is now three different things.** **Implemented and pushed:** the Prisma models `Segment`, `ProductType`, `ProductSegment` and `SegmentProductType`, plus nullable `Product.productTypeId` (`onDelete: Restrict`), in migration `20260812160853_add_product_taxonomy_v2`; and the Product Detail increment (B1), where `GET /products/:slug` returns `segments[]` (`name`, `slug`, ordered by `sortOrder` then a stable id tiebreaker) and `productType` (`name`, `slug`, or `null`) — internal ids never reach the response — with `ContentEntityType` extended by `Segment` and `ProductType` so both carry localized names and slugs through the existing `ContentTranslation` mechanism. **Implemented and pushed (B2):** the `GET /products?segment=` and `?productType=` filter contract from ADR-008 — `ProductListQuery` accepts both parameters alongside `category`, `q`, `page`, `limit`, `sort`, with locale-aware slug resolution (the requested locale's translated slug first, the entity's own slug second; the default locale skips the translation lookup), AND composition against `category` and `q`, filtering applied before pagination, and 400 `VALIDATION_ERROR` on an unknown slug. The filters are **list-only** — `PRODUCT_SELECT` is unchanged and the list response still exposes no taxonomy fields. `type` remains a rejected parameter name, and multi-value taxonomy filtering (`?segment=a,b`) remains deferred. **The catalog reference-data mechanism is implemented and pushed:** `prisma/seed-catalog.ts`, invoked explicitly as `pnpm seed:catalog` — dedicated and idempotent, deliberately not wired into `prisma db seed`, which stays locale-only. It upserts by `slug`, preserves `Segment.id`, never deletes, refuses to run unless `current_database()` is `sam_platform`, and hard-aborts if a Segment row carrying the slug `other` is ever found. The eight approved Segment rows were applied to the local DEV `sam_platform` during that gate and a rerun was verified idempotent; `Other` remains vocabulary rather than a persisted row. **The Product Type vocabulary is approved as of 31 August 2026 — [ADR-020](./docs/ADR/ADR-020-product-type-vocabulary-and-finder-scope.md).** Eight slugs and display names (`engine-oils` Engine Oils, `industrial-oils` Industrial Oils, `lubricant-additives` Lubricant Additives, `gear-oils` Gear Oils, `marine-oils` Marine Oils, `hydraulic-oils` Hydraulic Oils, `antifreeze-coolants` Antifreeze Coolants, `greases` Greases) are approved vocabulary, and showing them as a filter axis in the public Product Finder is approved. The set is closed; a ninth needs its own approval. Product Type stays a separate classification axis and does **not** replace or rename the six Product Families. **ADR-020 approves nothing else** — not translations, not technical claims, not new or changed per-product assignments, not Product Type pages, indexability or `GET /product-types`, and it certifies no implementation. This sentence previously read "No ProductType row, and no ProductSegment or SegmentProductType membership, has been created — the Product Type vocabulary is still unapproved"; the first half restated a dated 14 August 2026 local-DEV observation as a standing fact, and **no `SegmentProductType` membership row exists**. Cite ADR-020 for this vocabulary's authority rather than an earlier import approval: PRODUCT-DATA-2C-A ratified identities and PRODUCT-DATA-2C-B2B approved running the import, and the reviewed records show no closure of ADR-008's deferral — which is not the same as establishing that no earlier approval was given. ADR-020 neither asserts nor denies one. **Implemented frontend-side, and this line previously said none of it was.** The **frontend Product Detail route and shared template exist** — `apps/web/src/app/[locale]/products/[slug]/page.tsx` and `apps/web/src/features/products/detail/product-detail-template.tsx`, added 15 August 2026 and completed 28 August 2026. **Frontend taxonomy integration exists** on the public Product Finder: commit `b9b305e` completes the Product Type filter end to end — `?productType=` read off the URL, passed to `GET /products`, drawn as a third chip row from the eight ADR-020 §1 slugs and display names, with its own unrecognised-filter state — and adds URL-based pagination, `?page=`, with Previous/Next links, an out-of-range page state, and a page reset on every filter and search change. The Finder sends no `limit` and no `sort`, so the endpoint's `limit=20` and `sort=name` stand. **ADR-020 §2 is the approval for that surface**; ADR-020 certifies no implementation, and this line records what the repository contains rather than a review of it. **Still not implemented and deferred:** standalone `GET /segments` / `GET /product-types` endpoints, Segment and Product Type pages, sitemap/SEO records for them, and any Payload taxonomy collection. Translated taxonomy vocabulary also remains unapproved (ADR-020 Non-Goals), so the Finder's filter labels are the approved English names in every locale. ADR-007 records its own Non-Goals, Deferred Decisions (Grade / Variant modelling, `productCode`, the `Other` Segment, Industry/Marine naming, per-Segment Product Type lists) and the documentation conflicts it deliberately leaves open. Treat it as frozen alongside ADR-001 through ADR-006 — and alongside [ADR-008](./docs/ADR/ADR-008-b2-filter-contract-and-segment-vocabulary.md), which closes two of ADR-007's open items (the Segment slugs, and `Other` — nine approved names, eight persisted Segments, `Other` is vocabulary rather than a row) and freezes the B2 filter contract without implementing it. The per-Segment Product Type lists and every other deferred item stay open — but **not the Product Type vocabulary**, which ADR-020 §1 closed on 31 August 2026 and which this sentence went on listing as open.
- **RESOLVED — the flat Product vs Product Family URL collision is closed by [ADR-010](./docs/ADR/ADR-010-products-slug-namespace-and-collision-policy.md) (accepted 13 August 2026).** ADR-008's Deferred entry and the same item in ADR-009's "still open" list are superseded; **ADR-007, ADR-008 and ADR-009 are unmodified**. What is frozen: Product Family and Product Detail deliberately **share one namespace**, `/{locale}/products/{slug}`, with both URL shapes unchanged and neither `/products/categories/` nor `/products/p/` introduced; the eventual frontend route is **one shared dynamic route**, `app/[locale]/products/[slug]/page.tsx`, with **one** discriminator (sibling `[categorySlug]`/`[productSlug]` routes are not expressible in the App Router anyway); **Product Family wins** at runtime, and the stronger rule is that **colliding data is invalid** — a Product may not use a Product Family slug; the namespace is **symmetric**, spanning the union of base `Category` slugs, base `Product` slugs, and translated slug values of both, so a colliding Category is exactly as invalid as a colliding Product, and ambiguous translated-slug resolution is invalid data; `finder`, `segments` and `types` are **reserved** against all four of those; **durable enforcement must exist before** the first real Product write path, Product reference-data population, or the first `Category`/`Product` translated-slug row, with the mechanism **deliberately not selected at the time** (registry table, Postgres constraint trigger, or application validation — and application validation alone is explicitly _not_ equivalent to a database-enforced invariant; **that deferral is now closed by [ADR-011](./docs/ADR/ADR-011-products-slug-namespace-enforcement.md)**, see the entry below); **infrastructure failure must never be converted into a canonical-content 404**, with fixture fallback allowed on the Family branch while the six-family registry is authoritative, and a server-failure path rather than a 404 on the future Product branch; initial `fa`/`ar` Family URLs may carry the **default-locale English slug** (`/fa/products/base-oils`), which is **not** a localized slug and must never be described as one; and proof routes transition in the fixed order implement → validate → redirect → remove. **Implementation status: none.** ADR-010 authorizes no route, no middleware, no Product Detail, no enforcement mechanism, no schema change and no dependency — every one of those remains its own gate. Deferral of the enforcement mechanism rests on a **scoped local-DEV fact recorded in the ADR** (zero Products, zero Category/Product translated-slug rows as of 13 August 2026) and expires on the first write in the list above, not on a date.

- **RESOLVED — the slug-collision enforcement mechanism is selected by [ADR-011](./docs/ADR/ADR-011-products-slug-namespace-enforcement.md) (accepted 14 August 2026) and implemented by migration `20260814120000_add_product_slug_namespace_registry`.** This closes ADR-010 §6's deferral; **ADR-007 through ADR-010 are unmodified**. What is frozen: a shared **`ProductSlugClaim` registry**, whose normalized `slugKey` — `lower(normalize(value, NFC))` — is a unique key and therefore the **race-safe authority**; claims are **maintained by the database** through statement-level `AFTER` triggers with transition tables on `categories`, `products` and `content_translations`, never written by application code; **release-old before claim-new**, because reassigning a translation to another entity is a valid transition that claim-first would wrongly reject; release is **recomputed from the source tables**, never refcounted, so a claim survives while any source row of the same owner still maps to the key; **no advisory locks** — the unique index serializes, a cross-transaction `A↔B` rename may deadlock and that is detected and safe, and one-statement slug swaps stay unsupported exactly as the existing unique keys already make them. Three invariants: **INV-1** one normalized key belongs to exactly one entity globally across all locales, while that entity may reuse it across its own base and localized slugs; **INV-2** nothing may normalize to `finder`/`segments`/`types`; **INV-3** a translated slug may not claim namespace for a `Category`/`Product` that does not exist — enforced in the trigger for those two types only, **not** as a polymorphic FK redesign. INV-1 is **intentionally stricter than the theoretical minimum** (per-locale uniqueness over each locale's effective namespace would suffice) — chosen for invariant simplicity and base-slug-fallback compatibility, it can reject non-ambiguous cross-locale reuse, and relaxing it later needs a migration plus its own decision. Normalization is NFC + lower-case and **nothing more**: deeper `fa`/`ar` confusable folding (ZWNJ, Arabic-Indic digits, yeh/kaf variants) stays deferred to the gate that approves `fa`/`ar` slug vocabulary. Reserved vocabulary authority chain: **ADR-010 §4 → a SQL `product_slug_reserved()` function (DB enforcement copy) → a TypeScript constant (declaration only) → a drift test**. Installation is atomic: lock sources → create → build the complete candidate set → validate (reserved, cross-owner, orphan) → **generic** backfill of every existing base and translated slug → arm → assert coverage → commit, with validation aborting rather than picking a winner and **no DEV-specific six-row backfill**. Future write contract: reserved → 400 `VALIDATION_ERROR`, occupied → 409 `CONFLICT`, `field: slug`, owning entity never disclosed. **ADR-011 also records a factual correction to ADR-010's Context**: facts 2 and 3 claim translated slug values carry no uniqueness, but `content_translations_unique_slug` — unique on `(entity_type, locale, value)` where `field = 'slug'` — has existed since `0_init` and is live. ADR-010's **Decision** is unaffected, ADR-010 is **not edited**, and that index is **retained** rather than superseded. The migration, Prisma model, reserved-slug drift coverage and database verification now provide the implementation evidence; Product writes have subsequently occurred only for explicitly non-authoritative demo/import data.

- **RESOLVED — the Admin Dashboard has a home.** It's an application area inside `apps/web` at `app/(admin)/admin/*`, **outside** the `[locale]` segment (so its URLs are `/admin/...`, never `/en/admin/...` — it must never be indexed, and staying outside `[locale]` makes that structural rather than a list of exclusions). It exists because Payload's admin UI structurally cannot manage `sam_platform` data under ADR-002. Split: Prisma-owned data (catalog, blog, submissions, users, locales, redirects) → Admin Dashboard; Payload-owned editorial content → Payload admin. Full spec: `docs/API_CONTRACT_FINAL.md` §2.10–2.11, `docs/ARCHITECTURE.md`, `docs/SECURITY.md`.
  **RESOLVED — the blocker this surfaced is closed.** Confirmed by the user on 7 August 2026: **Payload Admin uses separate authentication.** Payload keeps its own admin users in `sam_cms`, reached at `cms.<domain>/admin`; NestJS does not manage Payload sessions; **no SSO bridge will be implemented**; no Payload accounts are synced from `User`; authentication cookies are never shared between `samgp.com` and `cms.samgp.com`. Payload also maintains **its own role model** — minimum `Admin` and `Content Manager` — mirroring the CMS-facing RBAC rules, with the **certification publish gate enforced in Payload's own permissions**. Recorded as **ADR-006** (`docs/ADR/ADR-006-payload-admin-authentication.md`), which completes the question ADR-005 left open without amending ADR-005 itself; treat it as frozen alongside ADR-001/002/003/004/005. Reconciled across `ARCHITECTURE.md`, `SECURITY.md`, `API_CONTRACT_FINAL.md`, `PROJECT_HANDOFF.md` and `ROADMAP.md`. **Step 6 / M2 unblocked.** Three consequences to carry forward: staff sign in twice (deep-linking mitigates navigation, not the second login); **argon2id (ADR-004) applies to platform identity only** — Payload hashes its own admin passwords natively; and **Payload account lifecycle is manual and is an offboarding risk** — revoking a platform user does not revoke CMS access, so creating/reviewing/disabling/removing Payload accounts is a mandatory part of CMS onboarding and offboarding. No automation for this is being built.

## Known environment oddities

- **Resolved** — the monorepo, workspace install, and tooling are all in place. Git is initialized with a remote; `pnpm install` has been run and `pnpm-lock.yaml` is committed; ESLint/Prettier/Husky + lint-staged are wired; CI Phase 1 (`.github/workflows/ci.yml`) validates **every pull request and every push to `main`** — its triggers are exactly `pull_request` and `push: branches: [main]`, so a direct push to a feature branch such as `feature/design-system` runs no workflow and receives no check. `packages/tsconfig`, `eslint-config` and `types` are config/type packages; `packages/ui` and `packages/config` now hold the frontend design system (see below). A `.gitattributes` file now pins LF line endings repository-wide — without it, a Windows checkout converted `docker/postgres/init/01-init-databases.sh` to CRLF, which breaks the script inside the Postgres container and would leave the ADR-002 boundary silently uncreated.
- **Resolved** — the Docker development stack exists and runs: `docker-compose.yml` (`postgres`, `minio`, `minio-init`, `nginx`), `docker-compose.override.yml`, Nginx templates, and the Postgres init script. `scripts/verify-db-isolation.sh` passes 4/4, so **ADR-002 isolation is verified, not assumed**. Re-run it after any change to the init script or the Postgres volume — that script only runs on first initialisation of an empty volume.
- **Resolved** — the database and backend layers exist. Beyond the public locale, catalog, blog, content and SEO reads, `apps/api` now includes the two public lead-write flows, email notification integration, JWT/RBAC, refresh-session and account-status handling, the read/write lead workflow, guarded catalog import, and the technical-review API. The catalog technical-data foundation, import identity/apply contract and review boundaries are frozen by ADR-014 through **ADR-018** — ADR-018 (Accepted, listed in [`docs/ADR/README.md`](./docs/ADR/README.md)) adds named, bounded and transactional incremental catalog patches, and is the current end of that boundary. Public and protected endpoints remain separated; NestJS is still the only API surface.
- **Resolved** — the frontend design system foundation exists (step A-2), in `packages/ui` and `packages/config`: tokens authored in TypeScript and generated into a Tailwind v4 theme, 13 Server-Component primitives, a twelve-column editorial grid, a specification primitive, and four scroll-driven CSS reveal patterns. No animation library. The full record is [docs/design/DESIGN_SYSTEM.md](./docs/design/DESIGN_SYSTEM.md) — read it before touching a token or adding a primitive.
- **Current as of 30 August 2026, evidenced against commit `6b82485`** — `apps/web` serves the canonical localized public routes and the BFF-backed Admin surface. `apps/cms` is a Payload 3 application connected only to `sam_cms`; Media/SEO foundations and **four** company Globals — `AboutUs`, `CustomizedSolutions`, `QualityCertifications` and `ContactUs` — are implemented end to end through the NestJS Content module (`apps/cms/src/payload.config.ts` registers exactly those four). **`ContactUs` is a bounded contact-channel slice, not a Contact page content model**: nine optional, nullable fields — main and sales phone, general and sales email, WhatsApp/LinkedIn/Instagram/Telegram URLs, and a localized address — allow-listed by the NestJS projection, with empty channels never rendered, a Global holding nothing answering `available: false` rather than 404, and no editorial body content of any kind. **No unverified contact value may be invented, guessed or published**; none was invented or seeded at `6b82485`, and supplying confirmed values is an editor action in Payload. `apps/web` also has the Admin session shell, lead inbox/workflow and catalog technical-review queue/detail screens. **Phase C review decisions are implemented**: eligible Specification and Product Claim detail screens expose explicit approve, reject and return-to-review controls through a Server Action; `status` plus the versioned review hash provide optimistic concurrency; blocked approval, stale conflicts, superseded subjects and API failures remain distinct; and the browser receives no token or direct NestJS write path. **The source-capture operator gate is also implemented**: `pnpm catalog:sources:capture` inspects an explicitly supplied local artifact, requires dry-run or separately confirmed apply, records only immutable SHA-256/size/media metadata, and attaches it one-way to an existing `SourceDocument` in a SERIALIZABLE transaction. It never stores source bytes, fetches a locator, approves technical data or exposes a locator. No live database capture has been executed. Still absent: application Dockerfiles, CI Phases 2–3, production deployment, approved commercial Product data, approved legal content, the remaining CMS model, and several contracted submission/Admin surfaces. Root `README.md` remains empty.

## How to re-orient in a fresh session with no conversation history

1. Read `CLAUDE.md`, then this file.
2. Check `docs/ROADMAP.md`'s "Current Status" for what phase the project is actually in right now (this file's own status notes above may lag if bootstrap has since happened — trust `ROADMAP.md` over this file for current state, and update both if you're the one moving the project forward).
3. Read `docs/ADR/README.md` for the decision log before proposing anything that touches monorepo tooling, database topology, API integration, or CMS boundaries — those four already have a settled answer.
4. If asked to do something that seems to contradict a frozen decision, cite the ADR and ask before proceeding.
