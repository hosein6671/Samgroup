// The platform's PostCSS configuration is shared, not per-app, so Tailwind's plugin set
// cannot drift between apps/web and any future consumer of the design system.
export { default } from "@sam-group/config/postcss";
