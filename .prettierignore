# Generated / vendored — never reformat
node_modules/
dist/
build/
out/
.next/
.turbo/
coverage/

# Emitted by packages/ui's token build from src/tokens/*.ts. Generated code, and the
# generator owns its formatting — reformatting it would produce a diff on every build.
packages/ui/src/tokens/theme.generated.css

# Prisma 7 emits the client into the repository rather than node_modules,
# because an explicit generator output path is now required. It is generated
# code — Prettier neither reads .gitignore nor should reformat it.
apps/api/src/prisma/generated/

# Payload emits both of these into the repository — `payload generate:types` and
# `payload generate:importmap` — and the build needs them there. Generated code,
# and the generator owns its formatting: reformatting would produce a diff on
# every regeneration, exactly as for the Prisma client above.
apps/cms/src/payload-types.ts
apps/cms/src/app/(payload)/admin/importMap.js

# Lockfile is tool-owned; reformatting it produces noise and diff churn
pnpm-lock.yaml

# Binary source material (see docs/PROJECT_HANDOFF.md §3 "Source material")
*.xlsx
